# This file is needed in case you deploy this file to Google Compute Engine.
version: v1
containers:
  - name: github-issue-mover
    image: nicolasgarnier/github-issue-mover
    ports:
      - name: http
        hostPort: 80
        containerPort: 8080
